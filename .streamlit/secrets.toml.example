# Public / Streamlit Cloud: paste these two entries into the app Secrets UI (or rename this
# file to secrets.toml for local testing of remote URLs). Do not commit real URLs or base64.
#
# When both keys are present, the app uses secrets mode automatically.

GDM_DATA_XLSX = "https://example.com/path/data.xlsx"
GDM_SALES_VAR_XLSX = "https://example.com/path/salesVariability.xlsx"
